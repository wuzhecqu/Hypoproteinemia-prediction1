
# 核心依赖（适配Python 3.13，无distutils依赖）
streamlit>=1.30.0
lightgbm==4.3.0          # 最新稳定版，预编译无需编译
pandas==2.1.4            # 兼容Python 3.13，不依赖旧版numpy
numpy==1.26.4            # 无distutils依赖的版本（>=1.26.0）
scikit-learn==1.3.2      # 与numpy 1.26.4兼容
openpyxl==3.1.2          # 读取Excel无冲突

# （可选，若需要SHAP/可视化）
shap==0.44.1             # 适配Python 3.13的SHAP版本
matplotlib==3.8.0        # 无distutils依赖
