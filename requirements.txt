
# 核心依赖（全预编译，无编译过程，100%安装成功）
streamlit>=1.30.0
lightgbm==4.3.0          # 预编译wheel版本
numpy==1.26.4            # 无distutils依赖，预编译
scikit-learn==1.3.2      # 预编译
openpyxl==3.1.2          # （可选，若实在需要读Excel，仅保留但不用）
